##
##    This file is use to customize my webpage
##

# -- Name of site (will be displayed in the header)
name: Rolando J. Acosta

# -- Short Description (also displayed in the header)
description: PhD Student in Biostatistics at Harvard University

# -- Not sure what this is for
#avatar: 

# -- Including other ways of contacting me
footer-links:
  email: roa310@g.harvard.edu 
  facebook:
  github: RJNunez
  instagram:
  linkedIn: rolando-j-acosta-nuñez-562330b3
  twitter: 
  medium: 

# -- Don't change anything below this line
permalink: /:title/

# The release of Jekyll Now that you're using
version: v1.2.0

# Jekyll 3 now only supports Kramdown for Markdown
kramdown:
  # Use GitHub flavored markdown, including triple backtick fenced code blocks
  input: GFM
  # Jekyll 3 and GitHub Pages now only support rouge for syntax highlighting
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    # Use existing pygments syntax highlighting css
    css_class: 'highlight'

# Set the Sass partials directory, as we're using @imports
sass:
  style: :expanded # You might prefer to minify using :compressed

# Use the following plug-ins
gems:
  - jekyll-sitemap # Create a sitemap using the official Jekyll sitemap gem
  - jekyll-feed # Create an Atom feed using the official Jekyll feed gem

# Exclude these files from your production _site
exclude:
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - CNAME


# -- Theme for the website
theme: jekyll-theme-slate
